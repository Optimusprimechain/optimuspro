import React from 'react';

const PageTitle = ({ title }) => (
  <div className=" my-3 sm:my-4">
    <h2 className="text-gray-800 font-bold sm:text-3xl text-2xl">
      {title}
    </h2>
  </div>
);

export default PageTitle;
